# Markdown 结构化修复工具实施计划

## 目标

依据 `docs/Markdown结构化修复工具功能需求与技术方案.md` 实现全部功能，并通过项目测试与关键功能验证。

## 阶段

| 阶段 | 状态 | 说明 |
|---|---|---|
| 1. 需求与现状盘点 | 已完成 | 已阅读完整方案；当前目录没有既有应用源码 |
| 2. 架构与任务拆解 | 已完成 | 已建立 TypeScript 核心、Vue/CodeMirror UI、测试的模块边界 |
| 3. 功能实现 | 已完成 | 已实现文档、结构、规则、补丁、历史和 UI 主链路 |
| 4. 自动化测试与修复 | 已完成 | Vitest、TypeScript/Vite 构建、Playwright 均已通过 |
| 5. 最终验证与交付 | 已完成 | 完成最终全量前端验证并生成生产构建 |
| 6. 桌面文件工作流改造 | 已完成 | 打开、保存、另存为、差异基线和统一编辑历史均已实现、测试并重新编译 |
| 7. 手动与自动修复模式 | 已完成 | 已实现 RepairPlan、规则分类、选区保护、自动预览、持久化设置和完整验证 |
| 8. 工作台与菜单重构 | 待最终打包 | 功能、单元测试、端到端测试与前端构建已完成；Tauri 打包受系统使用额度限制待重试 |
| 9. 主题、工作台可调布局与设置体验改造 | 已完成 | 深浅主题、块级公式、历史菜单、设置侧栏和修复菜单重构，并完成构建及自动化验证 |
| 10. 编辑器主题、跳转标记与 Windows 启动体验 | 已完成 | 代码和前端验证完成；Tauri 发布包无法覆盖正在使用的 mdtool.exe，需关闭运行中的程序后重建 |
| 11. 修复规则、章节导航与菜单交互升级 | 已完成 | 已扩展公式/间距/章节编号规则，移除置信度，并重构规则设置和章节树交互；测试与构建通过 |
| 12. 修复项层级与章节编号作用域 | 已完成 | 已完成修复项树、菜单拆分、统一说明，以及全文/选中章节编号规则对齐；测试与构建通过 |
| 13. 全文编号确认与其他修复菜单布局 | 已完成 | 已完成一级标题排除单选、菜单确认弹窗和其他修复纵向布局；测试与构建通过 |
| 14. 规则、文件生命周期与三栏工作台回归修复 | 进行中 | 实现范围编号、文件保存/打开/关闭保护、编辑器性能与三栏同步/交互修复 |
| 15. 修复预览标识、位置同步、文件兼容与侧栏交互 | 已完成 | 已实现本轮六组回归与交互需求，核心单测、构建与端到端测试均通过 |
| 16. 桌面端拖拽、撤销位置与源码右键菜单回归 | 已完成 | 已修复 Pointer 拖拽、撤销滚动位置、桌面端格式报错、折叠章节高亮和源码右键分组菜单；已构建备用 exe |
| 17. 编辑闪屏与只读视图右键菜单回归 | 已完成 | 已限制外壳文本选择、菜单自动向内展开、格式校验前置，并为三个只读视图提供仅复制菜单；测试通过待重新打包 |
| 18. 启动文件、间距删除规则与白屏优化 | 已完成 | 已支持启动参数打开文件、三类删除空格规则与首屏后显示窗口；测试和备用 Tauri 发布构建通过 |
| 19. 启动参数文件读取授权修复 | 已完成 | Rust 仅读取启动参数白名单中的文件并回传前端载入；核心、前端与 Tauri 发布构建通过 |

## 验收标准

- 需求文档列出的功能均有对应实现。
- 项目可按文档指定技术栈正常构建、运行。
- 自动化测试全部通过；无法自动化的关键路径有可复现验证记录。

## 错误记录

| 时间 | 问题 | 处理 |
|---|---|---|
| 2026-07-11 | Git 命令未将当前目录识别为仓库，尽管文件系统列出了 `.git`。 | 以文件系统与项目自身构建配置为准，不执行 Git 相关验收。 |
| 2026-07-11 | 未检测到 pnpm 与 Rust cargo。 | 使用 npm 执行前端依赖和测试；Tauri 原生端仅保留兼容的项目配置，无法在当前环境编译验证。 |
| 2026-07-11 | Playwright 内置 Web Server 在 Windows 下未能回收子进程。 | 改由 PowerShell Job 临时启动 Vite；测试结束时正常停止该 Job。 |
| 2026-07-11 | Tauri 生产链接输出中文 linker warning。 | 编译正常完成，产物已生成；该 warning 不影响功能。 |
| 2026-07-11 | 最终 Tauri 打包请求被平台使用额度限制拒绝。 | 不绕过限制；等待额度恢复后重新执行 `npm run tauri:build`。 |
| 2026-07-11 | Tauri 构建无法删除 `src-tauri/target/release/mdtool.exe`（os error 5）。 | 可执行程序正在被占用；不强制终止用户正在运行的软件，关闭后即可重新运行构建。 |
| 2026-07-12 | 第 15 阶段构建中 `restoreCenterPosition` 的不可达 fallback 被 TypeScript 缩窄为 `never`。 | 删除该 fallback；已挂载的 `v-show` 编辑器始终使用行锚点恢复。 |
| 2026-07-12 | 端到端测试以 `.cm-content` 定位输入框，而 `v-show` 使只读修复编辑器也保持挂载，触发 strict-mode 冲突。 | 更新选择器为非只读源码编辑器，保持测试只针对可编辑内容。 |
| 2026-07-12 | 更新输入选择器后，端到端仍引用已重命名的编号菜单与非精确“修复预览”按钮。 | 按当前 UI 语义更新为精确导航按钮、范围编号确认弹窗与规则名预览标签。 |
| 2026-07-12 | Tauri 无法覆盖 `src-tauri/target/release/mdtool.exe`（os error 5）。 | 该 exe 正在运行；未强制关闭，改以 `CARGO_TARGET_DIR=src-tauri/target-close-fix` 成功生成备用发布 exe。 |
| 2026-07-12 | Tauri 无法覆盖 `src-tauri/target-close-fix/release/mdtool.exe`（os error 5）。 | 用户正在使用此前的备用程序；不强制关闭，改用新的独立 Cargo 目标目录构建。 |
